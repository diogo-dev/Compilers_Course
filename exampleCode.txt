program example;
    var x, y: integer;
    var verificador1, verificador2: boolean
    begin
        x := 1111111110;
        y := x + 2345123 - .33;
        if y > x then
            write(y);
        else
            write(x);
        
        if verificador1:
            write(verificador2)
    end.